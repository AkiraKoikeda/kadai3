<%= flash[:notice] %>
<%= render 'books/list', book: @book %>

<h1>Books</h1>
<% @books.each do |book| %>
<div>

  <%= link_to book_path(book.id) do %>
   <%= book.book_name %>
  <% end %>
  <p><%= book.caption %></p>
</div>
<% end %>

